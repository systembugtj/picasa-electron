import contextMenu from 'picasa/utils/context-menu';
import { module, test } from 'qunit';

module('Unit | Utility | context menu');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = contextMenu();
  assert.ok(result);
});
