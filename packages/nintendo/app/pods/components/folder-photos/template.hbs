
{{#if hasFolders}}
  {{#each foldersWithThumbnail as |folder|}}
    {{folder-card root=folder.cwd images=folder.images inViewportName=moveTo onRefreshThumbnail=(action "handleRefreshThumbnail")}}
  {{/each}}
{{/if}}